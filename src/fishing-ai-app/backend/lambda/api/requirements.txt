# requirements.txt (API Lambda)
# AWS SDK for Python。DynamoDB へのアクセス（boto3.resource("dynamodb")）に使用する。
# Lambda ランタイムには標準搭載されているが、ローカル開発・sam build のために明示している。
boto3
# postChatHandler（AIチャット）がGemini APIをマルチモーダル呼び出しするために使用
google-generativeai